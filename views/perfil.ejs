<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Perfil</title>
</head>
<body>
    <h2> <%= Usuarios.nome %>  </h2>

    <button class="btn-historico" id="btn-hist">Historico</button>
    <!-- Essas duas divs de statusPedido e historico sao eventos, so aparecem ao clicar na opcao -->
    <div id="statusPedido">
        <p>Status do Pedido!!!!</p>
    </div>

    <script>
        let elemento = document.getElementById("statusPedido");
        elemento.style.display = "none";

        document.getElementById("btn-hist").onclick = function () { myFunction() };
        function myFunction() {
            if (elemento.style.display === "none") {
                elemento.style.display = "block";
            } else {
                elemento.style.display = "none";
            }       
        }
    </script>
</body>
</html>